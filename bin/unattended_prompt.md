Run the outreach skill in **unattended mode** (SKILL.md, "Unattended mode"). There is no human in this session: follow that section's rules exactly, never ask a question, never wait for input, and never send anything. Start by running `python3 outreach_run.py budget`; stop when it says nothing may be spent. When you finish, write the run summary to {SUMMARY_PATH} using the summary format in that section, then end the session.
